# OpenWeatherMapAPI
Troy Nelson Open Weather Map API Submission

To set up the application place your OpenWeatherMap API key within a .env file. Flace this file in the same location as the python files to run the application. 

You may need to run the following command in the terminal: pip install python-dotenv

